# Backend

FastAPI service for fraud scoring and model serving.

## Run
1) Create and activate a virtual environment
2) Install dependencies from requirements.txt
3) Run: uvicorn app.main:app --reload --port 8000

## Endpoints
- GET /health
- POST /predict
